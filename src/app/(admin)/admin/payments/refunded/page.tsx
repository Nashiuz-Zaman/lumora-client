import { RefundedPaymentsMain } from "@/components/page-specific";

export const metadata = {
  title: "Refunded Payments | Admin Panel",
};

const RefundedPaymentsPage = () => <RefundedPaymentsMain />;

export default RefundedPaymentsPage;
