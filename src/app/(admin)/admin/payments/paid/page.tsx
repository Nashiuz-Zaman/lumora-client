import { PaidPaymentsMain } from "@/components/page-specific";

export const metadata = {
  title: "Paid Payments | Admin Panel",
};

const PaidPaymentsPage = () => <PaidPaymentsMain />;

export default PaidPaymentsPage;
