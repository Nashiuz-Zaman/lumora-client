import { UnderDev } from "@/components/shared";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Orders | Admin Panel",
};

const CancelledOrdersPage = () => <UnderDev />;

export default CancelledOrdersPage;
