import { UnderDev } from "@/components/shared";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About Us",
};

const AboutUsPage = () => <UnderDev className="!h-[70vh]" />;

export default AboutUsPage;
