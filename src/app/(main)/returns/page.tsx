import { UnderDev } from "@/components/shared";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Returns",
};

const ReturnsPage = () => <UnderDev className="!h-[70vh]" />;

export default ReturnsPage;
