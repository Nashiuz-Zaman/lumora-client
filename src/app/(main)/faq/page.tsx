import { UnderDev } from "@/components/shared";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQ",
};

const FaqPage = () => <UnderDev className="!h-[70vh]"/>;

export default FaqPage;
